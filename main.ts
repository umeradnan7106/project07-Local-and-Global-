//  Global Variable 

// global variable is using every where
let age = 18;
if(true) {
    console.log(age);
}

// Local variable

// in local variable we use variable only in block ("{}")

if (true){
    let firstName = "Umer";
    console.log(firstName);
}
// console.log(firstName);    // don't use after block

